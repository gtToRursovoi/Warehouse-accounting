<Window x:Class="ApplicationApp.View.Page.RegistrationViewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ApplicationApp.View.Page"
        mc:Ignorable="d"
        Title="RegistrationViewWindow" Height="450" Width="350"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
                Background="#F1EFE9"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="12*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <Button Content="❌" Background="#AF2B1E" Width="50" Height="50" HorizontalAlignment="Right" Margin="0,10,10,0" Click="Button_Click"/>

        </StackPanel>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1">
            <TextBlock Text="Login" Margin="36,0,0,4"/>
            <TextBox Text="{Binding Login}" Width="150" Margin="0,0,0,20"/>
            <TextBlock Text="Password" Margin="36,0,0,4"/>
            <TextBox Text="{Binding Password}" Width="150" Margin="0,0,0,20"/>
            <Button Content="Зарегистрироваться" Command="{Binding RegisterCommand}" Margin="10" Background="#6BC8A9" Width="200"/>
        </StackPanel>
    </Grid>
</Window>
