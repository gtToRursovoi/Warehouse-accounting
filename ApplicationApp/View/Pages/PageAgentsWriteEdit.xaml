<Page x:Class="ApplicationApp.View.Pages.PageAgentsWriteEdit"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ApplicationApp.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageAgentsWriteEdit"
      
      >

    <Grid>
        <StackPanel HorizontalAlignment="Center" Width="800">
        <TextBlock TextWrapping="Wrap" Padding="5" Text="Название агента"/>
            <TextBox x:Name="Name" TextWrapping="Wrap" Padding="5" Width="800" Text="{Binding Name}"/>

        <TextBlock TextWrapping="Wrap" Padding="5" Text="ИНН ровно 10 цифр"/>
            <TextBox x:Name="INN" TextWrapping="Wrap" Padding="5" Text="{Binding INN}" Width="800"/>

        <TextBlock TextWrapping="Wrap" Padding="5" Text="Юридический адрес"/>
            <TextBox x:Name="LegalAdress" TextWrapping="Wrap" Padding="5" Text="{Binding LegalAddress}" Width="800"/>

        <TextBlock TextWrapping="Wrap" Padding="5" Text="Директор" />
            <TextBox x:Name="Directro" TextWrapping="Wrap" Padding="5" Text="{Binding Director}" Width="800"/>

            <Button Content="Добавить" Margin="10" Background="#2E2E2E" Command="{Binding SaveCommand}"/>
            <Button Content="Отменить" Margin="10" Background="#2E2E2E" Click="close_click"/>
        </StackPanel>
    </Grid>
</Page>
