<Page x:Class="ApplicationApp.View.Pages.PageWarehouseProductsWrite"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ApplicationApp.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageWarehouseProductsWrite">

    <Grid>
        <StackPanel>
            <TextBlock TextWrapping="Wrap" Padding="5" Text="Склад"/>
            <TextBox x:Name="WarehouseId" TextWrapping="Wrap" Padding="5" Text="{Binding WarehouseId}"  Width="800"/>

            <TextBlock TextWrapping="Wrap" Padding="5" Text="Продукт"/>
            <TextBox x:Name="ProductId" TextWrapping="Wrap" Padding="5" Text="{Binding ProductId}" Width="800"/>

            <TextBlock TextWrapping="Wrap" Padding="5" Text="Колличество продукта на складе"/>
            <TextBox x:Name="CountProduct" TextWrapping="Wrap" Padding="5" Text="{Binding ProductCount}" Width="800"/>
            <Button Content="Сохранить" Margin="10" Background="#2E2E2E" Command="{Binding SaveCommand}"/>
            <Button Content="Отменить" Margin="10" Background="#2E2E2E" Click="close_click"/>
        </StackPanel>
    </Grid>
</Page>
