<Page x:Class="ApplicationApp.View.Pages.PageProducts"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ApplicationApp.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageProducts">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="0.2*" />
            <ColumnDefinition Width="15" />
        </Grid.ColumnDefinitions>
        <ListView x:Name="ProductsListView"
          ItemsSource="{Binding Products}"
          SelectedItem="{Binding SelectedProduct, Mode=TwoWay}"
          Grid.Column="1"
          BorderThickness="0"
          Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="2" Padding="5" CornerRadius="5">
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <!-- Картинка продукта -->
                            <Image Source="{Binding ImagePath}" Width="100" Height="100" Margin="5"/>

                            <!-- Информация о продукте -->
                            <StackPanel Orientation="Vertical" Margin="10,0,0,0">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" />
                                <TextBlock Text="Артикул:" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Articul}" />
                                <TextBlock Text="Минимальная цена:" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding MinPrice}" />
                                <TextBlock Text="Количество работников:" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding PeopleMake}" />
                                <TextBlock Text="Адрес производителя:" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding ManafacturAdrres}" TextWrapping="Wrap" />
                                <TextBlock Text="ID материала:" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding MaterialId}" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


        <StackPanel Grid.Column="3">
            <Button Height="40" Width="120" Margin="0,10,0,10" Content="Добавить" Background="#2E2E2E" Click="OpenAddPage"/>
            <Button Height="40" Width="120" Margin="0,10,0,10" Content="Изменить" Background="#2E2E2E" Click="EtitOpenPage"/>
            <Button Height="40" Width="120" Margin="0,10,0,10" Content="Удалить" Background="#2E2E2E" Command="{Binding DeleteCommand}"/>
        </StackPanel>
    </Grid>
</Page>
