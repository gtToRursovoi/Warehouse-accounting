<Page x:Class="ApplicationApp.View.Pages.PageSalesWriteEdit"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ApplicationApp.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageSalesWriteEdit">

    <Grid>
        <StackPanel Margin="20" Width="400">
            <!-- Агент -->
            <TextBlock Text="Агент:" Margin="0,5"/>
            <ComboBox ItemsSource="{Binding Agents}"
                      SelectedItem="{Binding SelectedAgent}"
                      DisplayMemberPath="Name"
                      Width="300"/>

            <!-- Продукт со склада -->
            <TextBlock Text="Продукт со склада:" Margin="0,10,0,5"/>
            <ComboBox ItemsSource="{Binding WarehouseProducts}"
          SelectedItem="{Binding SelectedWarehouseProduct}"
          Width="300"/>


            <!-- Количество продаж -->
            <TextBlock Text="Количество продаж товара:" Margin="0,10,0,5"/>
            <TextBox Text="{Binding SalesCount}" Width="300"/>

            <!-- Дата продажи -->
            <TextBlock Text="Дата продажи:" Margin="0,10,0,5"/>
            <DatePicker SelectedDate="{Binding SalesDate}" Width="300"/>

            <!-- Кнопки -->
            <Button Content="Сохранить"
                    Margin="0,20,0,5"
                    Width="150"
                    HorizontalAlignment="Left"
                    Command="{Binding SaveCommand}"/>
            <Button Content="Отменить"
                    Width="150"
                    HorizontalAlignment="Left"
                    Click="close_click"/>
        </StackPanel>

    </Grid>
</Page>
