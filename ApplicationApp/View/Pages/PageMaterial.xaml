<Page x:Class="ApplicationApp.View.Pages.PageMaterial"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ApplicationApp.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageMaterial">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="0.2*" />
            <ColumnDefinition Width="15" />
        </Grid.ColumnDefinitions>
        <ListBox ItemsSource="{Binding Materials}" 
         SelectedItem="{Binding SelectedMaterial}" Grid.Column="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="ID: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding Id}" Margin="0,0,10,0"/>
                        <TextBlock Text=" "/>
                        <TextBlock Text="Название: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding Name}"/>
                        <TextBlock Text=" "/>
                        <TextBlock Text="Количество материала: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding CountMaterials}"/>
                        <TextBlock Text=" "/>
                        <TextBlock Text="Тип матевриала: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding TypeMaterial}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Column="3">
            <Button Height="40" Width="120" Margin="0,10,0,10" Content="Добавить" Background="#2E2E2E" Click="OpenAddPage"/>
            <Button Height="40" Width="120" Margin="0,10,0,10" Content="Изменить" Background="#2E2E2E" Click="EtitOpenPage"/>
            <Button Height="40" Width="120" Margin="0,10,0,10" Content="Удалить" Background="#2E2E2E" Command="{Binding DeleteMaterialCommand}"/>
        </StackPanel>
    </Grid>
</Page>
