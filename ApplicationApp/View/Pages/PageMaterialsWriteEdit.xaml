<Page x:Class="ApplicationApp.View.Pages.PageMaterialsWriteEdit"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ApplicationApp.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageMaterialsWriteEdit">

    <Grid>
        <StackPanel HorizontalAlignment="Center" Width="800">
            <TextBlock TextWrapping="Wrap" Padding="5" Text="Название материала"/>
            <TextBox x:Name="Name" TextWrapping="Wrap" Padding="5" Width="800" Text="{Binding Name}"/>

            <TextBlock TextWrapping="Wrap" Padding="5" Text="Количество материала"/>
            <TextBox x:Name="INN" TextWrapping="Wrap" Padding="5" Text="{Binding CountMaterials}" Width="800"/>

            <TextBlock TextWrapping="Wrap" Padding="5" Text="Тип материала"/>
            <TextBox x:Name="LegalAdress" TextWrapping="Wrap" Padding="5" Text="{Binding TypeMaterial}" Width="800"/>

            <Button Content="Сохранить" Margin="10" Background="#2E2E2E" Command="{Binding SaveCommand}"/>
            <Button Content="Отменить" Margin="10" Background="#2E2E2E" Click="close_click"/>
        </StackPanel>
    </Grid>
</Page>
